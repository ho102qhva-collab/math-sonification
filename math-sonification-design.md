# 数学图形声觉化工具——产品设计方案

## 一、项目愿景

让盲人通过听觉感知数学图形，使数学不再因为"看不见"而变得陌生和困难。本工具将坐标系上一切可以写出解析式的数学表达式转化为音频信号，通过音高、立体声、音色、节奏等多维声觉通道，让盲人用户建立对数学概念的直觉理解。

---

## 二、核心设计原则

**最高优先级约束：用户必须能通过交互式探索，在30秒内建立起对一个函数的"数学理解"——不只是知道形状，而是知道零点、极值、单调性、定义域这些关键属性。sonification只是手段之一，必须与语音提示、键盘导航结合。**

---

## 三、声音映射体系

### 3.1 基础映射规则

| 数学维度 | 声觉通道 | 具体映射方式 |
|---------|---------|-------------|
| y 值 | 音高 | 对数频率映射，C3(131Hz)到C6(1047Hz)，y=0对应C4(262Hz) |
| x 值（扫描进度） | 时间轴 | 从左到右随时间推进 |
| x 位置（左右） | 立体声声像 | StereoPanner，x<0偏左耳，x>0偏右耳 |
| y 的正负域 | 音色 | y>0用正弦波（温暖纯净），y<0用方波或锯齿波（粗糙感） |
| 斜率绝对值 | 音量 | 斜率大→音量大，斜率接近零→音量最低，形成自然的"呼吸感" |

### 3.2 音高映射的关键参数

- 使用对数频率映射（半音等距），符合人耳感知。
- 频率范围：C3(131Hz) ~ C6(1047Hz)，共3个八度。
- y=0 对应 C4(262Hz)，即中央C，人耳最敏感区域。
- 超出映射范围的值（趋向无穷）：音高不再升高/降低，切换为"溢出"音效。

---

## 四、坐标感知系统

### 4.1 网格节拍（替代端点音效）

x轴上的整数点以等间距轻微节拍（tick）标记，用户可通过数节拍感知x坐标位置。x=0处使用区别于其他整数点的不同tick音效（如稍高频率的tick），使原点可辨识。

### 4.2 y=0 参考音

播放一根持续的、极轻的参考音线（固定在C4频率），用户可实时感知当前函数值距x轴的远近。当函数音高与参考音重合时，自然感知到"经过了x轴"。

### 4.3 特殊点音效体系

| 特殊点类型 | 音效设计 |
|-----------|---------|
| 过零点（y=0） | 短促高频click |
| 过y轴（x=0） | 短促低频thud |
| 极大值 | 升调"叮" |
| 极小值 | 降调"叮" |
| 拐点 | 音色发生微妙变化 |
| 垂直渐近线（→+∞） | 音高快速上升至极高后突然静音0.3秒 |
| 垂直渐近线（→-∞） | 音高快速下降至极低后突然静音0.3秒 |
| 跳跃不连续 | 短促"咔嗒"声标记断点，音高跳至新值 |
| 无定义点 | 极短的"空洞"音效（类似无线电静噪声） |
| 可去间断点 | 正常过渡，叠加轻微"缺口提示音" |

---

## 五、曲线类型分层处理

### 5.1 第一层：显式函数 y=f(x)

从左到右扫描x，直接映射音高和立体声。这是最基本的模式。

### 5.2 第二层：参数方程 x=f(t), y=g(t)

按参数t从小到大扫描。x映射到立体声声像，y映射到音高。椭圆、利萨如图形等走此路径。

### 5.3 第三层：极坐标 r=f(θ)

按角度θ从0到2π扫描。将极坐标换算为直角坐标后，x→立体声，y→音高。玫瑰线、心形线等走此路径。

### 5.4 第四层：隐函数 F(x,y)=0

先数值求解曲线，再将解参数化为路径，按路径扫描。对于多支曲线（如双曲线两支），每支单独播放，用不同音色区分。

### 5.5 坐标系类型的感知提示

每次切换坐标系时，播放明确的语音提示，并叠加独特的环境音变化：

- 直角坐标：干净无混响的声音
- 极坐标：带有轻微旋转感的声音（通过panning的缓慢循环实现）

确保用户始终保持空间方向感。

---

## 六、语音摘要层

在播放sonification之前，先用语音向用户报告函数的关键数学属性：

- 函数类型与表达式
- 定义域
- 零点数量及近似位置
- 极值点的位置和值
- 单调区间
- 对称性
- 渐近线（如有）

示例："这是函数y等于x的三次方减3x加1。定义域为全体实数。有三个实数零点，分别约在x等于负1.9、0.3和1.5。有一个极大值在x等于负1，y约等于3；一个极小值在x等于1，y约等于负1。"

这为盲人用户提供认知脚手架，使后续听音时知道要关注什么。

---

## 七、交互式导航系统

### 7.1 键盘控制方案

| 按键 | 功能 |
|------|------|
| 左/右箭头 | 沿曲线逐步移动位置（单步探索） |
| 上/下箭头 | 调整扫描精度/步长 |
| 空格 | 从当前位置开始连续播放 |
| 回车 | 语音报出当前精确坐标（如"x等于2.3，y等于4.7"） |
| Z | 跳转到最近的零点 |
| M | 跳转到最近的极值点 |
| Home / End | 跳到扫描范围的起点/终点 |
| D | 切换导数模式（听f'(x)的sonification） |
| Tab | 多函数叠加时在不同曲线间切换焦点 |
| + / - | 缩放：扩大/缩小当前扫描的x范围 |
| B | 在当前位置打书签 |
| Shift+B | 跳转到上一个书签 |

### 7.2 缩放系统

用户按+键可缩小x范围（"放大"细节），按-键可扩大x范围（"缩小"看全貌）。缩放时以当前位置为中心，语音提示新的x范围。

### 7.3 书签与语音备注

用户在探索过程中按B键打书签，系统记录当前坐标。书签支持语音备注（用户按B后说一句话，备注绑定到该坐标点）。跳回书签时自动播放备注。

---

## 八、分层感知策略（感知协议）

工具引导用户按以下顺序感知函数，而非让用户自行摸索：

1. **语音摘要**（第零遍）：听取函数关键属性的语音描述。
2. **快速扫描**（第一遍，约2秒）：获得整体形状轮廓，相当于"远看"。
3. **中速扫描**（第二遍，约5秒）：感知关键特征点（零点、极值等）。
4. **自由探索**（第三遍起）：用户使用键盘导航逐步探索感兴趣的区域。

默认一次扫描时长不超过4-6秒。连续探索模式中每10秒自动插入1秒静音间隔，防止听觉疲劳。

---

## 九、导数模式

按D键后，播放的不再是f(x)本身，而是f'(x)的sonification。通过此模式，用户可以直接听到：

- f'(x)>0 的区间（音高在参考音之上）= 原函数递增
- f'(x)<0 的区间（音高在参考音之下）= 原函数递减
- f'(x)=0 的位置（音高触及参考音）= 原函数的极值点

这对理解微积分概念极其有价值。

---

## 十、区域与面积感知

### 10.1 面积的听觉化

用"音频纹理密度"表示面积区域。在每个x位置，播放一个从y下界到y上界的"音高带"（和弦），覆盖的音高范围越大，听起来越"厚"。

### 10.2 定积分的感知

扫过的"音高带厚度"在感知上等同于面积的变化，让用户直觉理解定积分的几何意义。

---

## 十一、多函数叠加

### 11.1 同坐标系多曲线

不同函数使用不同音色的波形分别表示。按Tab键在不同曲线间切换焦点（前景/背景切换）。

### 11.2 交叉点标记

两条曲线的交叉点用特殊音效标记（区别于单函数的零点click）。

### 11.3 参照函数对比模式

输入一个函数后，系统自动识别其基础函数（如输入2sin(x)，识别出基础函数sin(x)）。基础函数用淡化的背景音持续播放，用户输入的函数用前景音播放，用户可直接听到差异。

适用场景：理解函数变换（平移、缩放、反射）。例如sin(x-π/2)和sin(x)同时播放，用户可听出"同样的旋律但晚了一拍"——这就是相位平移的听觉化。

---

## 十二、输入系统

### 12.1 预置函数库

提供分类整理的常用函数列表，用户从中选择后修改参数即可，无需手动输入表达式：

- **一次函数**：y=kx+b
- **二次函数**：y=ax²+bx+c
- **三次函数**：y=ax³+bx²+cx+d
- **幂函数**：y=xⁿ
- **指数函数**：y=aˣ, y=eˣ
- **对数函数**：y=log_a(x), y=ln(x)
- **三角函数**：sin, cos, tan, cot, sec, csc
- **反三角函数**：arcsin, arccos, arctan
- **圆**：x²+y²=r²
- **椭圆**：x²/a²+y²/b²=1
- **双曲线**：x²/a²-y²/b²=1
- **抛物线**：y²=2px
- **极坐标曲线**：心形线、玫瑰线、螺旋线等
- **参数方程**：摆线、利萨如图形等
- **分段函数**：阶跃函数、绝对值等

### 12.2 参数调节

每个预置函数暴露可调参数（如a, b, c, k等），用户可通过键盘增减参数值。每次修改后自动重新计算并播放，实现实时交互。

### 12.3 术语对齐

所有函数名称和语音提示严格对齐国内数学教材术语体系。若未来国际化，术语映射做成可配置模块。

---

## 十三、听觉疲劳管理

### 13.1 音色选择

优先使用柔和的正弦波或三角波作为默认波形。方波和锯齿波仅用于特定标记（如负值域的音色区分），避免长时间使用。

### 13.2 时长控制

- 单次扫描默认时长：4-6秒
- 连续探索模式：每10秒自动插入1秒静音间隔
- 用户可自行调整速度和间隔

### 13.3 触觉振动辅助（移动端）

利用手机振动马达作为第三信息通道，分担听觉负荷：

| 事件 | 振动模式 |
|------|---------|
| 函数值为负 | 持续轻微振动 |
| 经过零点 | 短振一下 |
| 经过极值点 | 长振一下 |

用户可选择关闭部分音效提示，以振动替代。

---

## 十四、个体听力校准

### 14.1 首次使用校准流程

工具首次启动时引导用户完成听力校准：

1. 播放一系列不同频率的音（覆盖C3到C6范围）
2. 每个频率用户反馈"能听到"/"听不到"/"不清楚"
3. 系统据此调整频率映射范围，避开用户的听力盲区
4. 校准结果保存为用户配置

### 14.2 校准的必要性

盲人群体中存在一定比例的多重感官障碍（先天性残疾群体尤其如此），部分用户可能在特定频段有听力下降。不做校准，工具对这部分用户可能完全失效。

---

## 十五、延迟处理与反馈确认

### 15.1 即时确认

用户按下播放键的瞬间，立刻播放一个极短的确认音（"嘀"），告知"收到指令，正在准备"。

### 15.2 计算延迟处理

- 计算时间<0.5秒：确认音后直接衔接sonification
- 计算时间0.5-1秒：确认音后短暂等待，自然衔接
- 计算时间>1秒：确认音后插入语音提示"正在计算"

### 15.3 设计原则

对盲人用户而言，延迟等于失控感——他们无法通过视觉确认"正在加载"。所有操作必须有即时的听觉反馈。

---

## 十六、错误处理

### 16.1 无效参数

当用户修改参数导致表达式无效时（如除以零）：

- 用语音明确告知错误类型和位置："参数a不能为零，当前表达式包含除以零错误"
- 自动回退到上一个有效状态
- 确保用户永远不会卡在无声的死状态中

### 16.2 设计原则

绝不使用视觉弹窗提示错误。所有错误信息通过语音传达，且必须具体（说明哪个参数、什么问题），而非笼统的"表达式无效"。

---

## 十七、低视力用户支持

完全无光感的全盲只占盲人群体的一部分，大量用户为低视力（能看到模糊的光影和大色块）。工具应同步显示：

- 高对比度、大色块的简化图形
- 粗线条绘制
- 大字号坐标标注
- 强烈的颜色对比
- 与sonification同步运动的光标/扫描线

此功能显著扩大用户群，且开发成本不高。

---

## 十八、教学引导系统

### 18.1 内置教学课程（核心功能，非可选）

| 课程序号 | 内容 | 教学目标 |
|---------|------|---------|
| 第1课 | y=1（水平线） | 理解"不变的音高 = 水平线" |
| 第2课 | y=x（直线上升） | 理解"音高上升 = 图形上升" |
| 第3课 | y=-x（直线下降） | 理解方向反转 |
| 第4课 | y=x²（抛物线） | 第一次感知曲率——"先降后升" |
| 第5课 | 引入网格节拍 | 学会听坐标标记音效 |
| 第6课 | 引入零点/极值音效 | 学会听特殊点提示 |
| 第7课 | y=sin(x) | 感知周期性 |
| 第8课 | 引入导数模式 | 理解"变化的变化" |
| 第9课 | 引入参照对比模式 | 理解函数变换 |
| 第10课 | 椭圆（参数方程） | 从函数扩展到曲线 |
| 后续课程 | 逐步引入更复杂的函数和操作 | ... |

### 18.2 奇点音效教学

在用户第一次遇到包含不连续点的函数之前，教学模块应单独教授每种奇点音效的含义，逐一演示并确认用户理解。

---

## 十九、自测与效果验证

### 19.1 自测模块

播放一段sonification后，向用户提出选择题以验证理解：

- "这个函数有几个零点？"
- "它在x=0处是递增还是递减？"
- "极大值大约在什么位置？"

### 19.2 产品迭代数据

自测结果是衡量sonification方案是否有效的核心数据来源。通过分析用户在哪些题目上正确率低，定向优化对应的声音设计。

---

## 二十、课堂协作与数据管理

### 20.1 教师端

教师可查看：学生当前在探索哪个函数、探索了哪些区域、自测成绩。

### 20.2 学生端

- 书签与语音备注可保存、回顾
- 探索记录可导出
- 支持课后复习

### 20.3 设计原则

如果工具只能实时听、不能回顾记录，在教学场景中的实用性大打折扣。

---

## 二十一、屏幕阅读器兼容性

### 21.1 基础要求

所有界面元素（按钮、菜单、状态信息）完整支持ARIA标签，兼容JAWS、NVDA、VoiceOver等主流屏幕阅读器。

### 21.2 音频冲突协调

| 模式 | 屏幕阅读器 | Sonification |
|------|-----------|-------------|
| 界面操作模式 | 正常工作 | 暂停 |
| Sonification播放模式 | 静音或压低音量 | 正常播放 |
| 语音摘要播放时 | 静音 | 暂停 |

模式切换通过用户按键明确触发，避免两种语音互相干扰。

---

## 二十二、技术选型建议

### 22.1 推荐方案

- **平台**：Web应用（覆盖面最广、无需安装、屏幕阅读器兼容性成熟）
- **音频引擎**：Web Audio API（支持精确频率控制、立体声声像StereoPannerNode、多种波形、实时合成）
- **数学计算**：math.js（符号计算与数值求值）
- **语音合成**：Web Speech API（语音摘要与提示）
- **移动端振动**：Vibration API

### 22.2 架构预留

虽然当前不做三维曲面（z=f(x,y)），但架构上预留扩展口（切片扫描方案），避免后期重构。

---

## 二十三、附录：完整音效清单

| 编号 | 音效名称 | 触发条件 | 描述 |
|------|---------|---------|------|
| S01 | 操作确认音 | 用户按下任何操作键 | 极短"嘀"声 |
| S02 | 网格节拍 | 扫描经过x轴整数点 | 轻微tick |
| S03 | 原点节拍 | 扫描经过x=0 | 区别于普通节拍的tick |
| S04 | y参考音 | 持续 | 极轻的C4持续音 |
| S05 | 零点click | y值过零 | 短促高频click |
| S06 | y轴thud | x值过零 | 短促低频thud |
| S07 | 极大值叮 | 经过极大值 | 升调叮 |
| S08 | 极小值叮 | 经过极小值 | 降调叮 |
| S09 | 拐点音色变化 | 经过拐点 | 音色微妙切换 |
| S10 | 正无穷渐近 | y→+∞ | 音高快速上升后静音0.3秒 |
| S11 | 负无穷渐近 | y→-∞ | 音高快速下降后静音0.3秒 |
| S12 | 跳跃不连续 | 跳跃间断点 | 短促"咔嗒"后音高跳变 |
| S13 | 无定义点 | 无定义位置 | 极短静噪声 |
| S14 | 可去间断 | 可去间断点 | 轻微缺口提示音 |
| S15 | 溢出音效 | y值超出映射范围 | 特殊提示音替代持续升降 |
| S16 | 交叉点 | 多函数交叉 | 区别于零点click的特殊音效 |
| S17 | 书签确认 | 用户打书签 | 短促确认音 |
| S18 | 坐标系切换 | 切换坐标类型 | 环境音变化+语音提示 |
| S19 | 正值域波形 | y>0 | 正弦波 |
| S20 | 负值域波形 | y<0 | 方波/锯齿波 |
| S21 | 计算中提示 | 计算超过1秒 | 语音"正在计算" |
| S22 | 错误提示 | 表达式无效 | 语音说明错误类型与位置 |
